#What is randomization in Python?
# Python Random module generates random numbers in Python. These are pseudo-random numbers means they are not truly random
import random

random_interger = random.randint(100, 1000)
print(random_interger) # intergers

random_float = random.random() * 2
print(random_float)
